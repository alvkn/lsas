<Window x:Class="LightSteamAccountSwitcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        x:Name="RootWindow"
        Title="Light Steam Account Switcher" Height="500" Width="850"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="CanResize">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <Style x:Key="ScrollBarThumb" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="true" />
            <Setter Property="IsTabStop" Value="false" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="rectangle" Background="#333333" CornerRadius="4"
                                Height="{TemplateBinding Height}" SnapsToDevicePixels="True"
                                Width="{TemplateBinding Width}" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="rectangle" Value="#555555" />
                            </Trigger>
                            <Trigger Property="IsDragging" Value="true">
                                <Setter Property="Background" TargetName="rectangle" Value="#777777" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false" />
            <Setter Property="Stylus.IsFlicksEnabled" Value="false" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Width" Value="8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="Bg" SnapsToDevicePixels="true">
                            <Track x:Name="PART_Track" IsDirectionReversed="true"
                                   IsEnabled="{TemplateBinding IsEnabled}">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.LineUpCommand}" Opacity="0" />
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.LineDownCommand}" Opacity="0" />
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumb}" />
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ContextMenu Style -->
        <Style TargetType="{x:Type ContextMenu}">
            <Setter Property="Background" Value="#111111" />
            <Setter Property="BorderBrush" Value="#333333" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="#EEEEEE" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="4">
                            <ItemsPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type MenuItem}">
            <Setter Property="Foreground" Value="#EEEEEE" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type MenuItem}">
                        <Border x:Name="Border" Padding="12,8" Background="Transparent" SnapsToDevicePixels="True"
                                CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="Icon" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="Shortcut" />
                                    <ColumnDefinition Width="13" />
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0" x:Name="Icon" ContentSource="Icon" Margin="0,0,10,0" />
                                <ContentPresenter Grid.Column="1" x:Name="HeaderHost" ContentSource="Header"
                                                  RecognizesAccessKey="True" />
                                <TextBlock Grid.Column="2" Text="{TemplateBinding InputGestureText}" Margin="10,0,0,0"
                                           Foreground="#888888" />
                                <Path Grid.Column="3" x:Name="Arrow" HorizontalAlignment="Right"
                                      VerticalAlignment="Center" Data="M0,0 L4,3.5 L0,7 Z" Fill="#888888"
                                      Visibility="Collapsed" />
                                <Popup x:Name="Popup" Placement="Right" IsOpen="{TemplateBinding IsSubmenuOpen}"
                                       AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                                    <Border Background="#111111" BorderBrush="#333333" BorderThickness="1"
                                            CornerRadius="6" Padding="4" Margin="4,0,0,0">
                                        <ItemsPresenter />
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#222222" />
                            </Trigger>
                            <Trigger Property="HasItems" Value="True">
                                <Setter TargetName="Arrow" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type Separator}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Separator}">
                        <Border Height="1" Background="#222222" Margin="0,4,0,4" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Window Button Style -->
        <Style x:Key="WindowBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="#888888" />
            <Setter Property="Width" Value="40" />
            <Setter Property="Height" Value="30" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#222222" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CloseBtn" TargetType="Button" BasedOn="{StaticResource WindowBtn}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C42B1C" />
                    <Setter Property="Foreground" Value="White" />
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <!-- Main Container -->
    <Border Background="#0A0A0A" CornerRadius="8" BorderBrush="#333333" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="*" />
                <RowDefinition Height="30" />
            </Grid.RowDefinitions>

            <!-- Custom Header -->
            <Grid Grid.Row="0" Background="Transparent" MouseDown="Header_MouseDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Logo/Title -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                    <TextBlock Text="LSAS" FontWeight="Bold" FontSize="18" Foreground="White" Margin="0,0,10,0" />
                    <TextBlock Text="Light Steam Account Switcher" Foreground="#666666" FontSize="13"
                               VerticalAlignment="Bottom" Margin="0,0,0,3" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                    <TextBlock Text="New version available!"
                               Foreground="#FF5555"
                               FontSize="13"
                               VerticalAlignment="Bottom"
                               Margin="0,0,10,0"
                               TextDecorations="Underline"
                               Cursor="Hand"
                               Visibility="{Binding IsUpdateAvailable, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock.InputBindings>
                            <MouseBinding Gesture="LeftClick" Command="{Binding OpenUpdateUrlCommand}" />
                        </TextBlock.InputBindings>
                    </TextBlock>
                </StackPanel>

                <!-- Actions & Window Controls -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,10,0">
                    <Button Content="🛈" Command="{Binding OpenAboutCommand}"
                            ToolTip="About"
                            Margin="0,0,10,0" Style="{StaticResource WindowBtn}" />

                    <Button Content="⚙" Command="{Binding OpenSettingsCommand}"
                            ToolTip="Settings"
                            Margin="0,0,10,0" Style="{StaticResource WindowBtn}" />

                    <Button Content="+ Add Account" Command="{Binding AddAccountCommand}"
                            Background="#2A475E" Foreground="#94D4F7" BorderThickness="0"
                            Padding="15,8" Margin="0,8,15,8" FontWeight="SemiBold" FontSize="13" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="6">
                                    <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center"
                                                      VerticalAlignment="Center" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#2E6087" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <StackPanel Orientation="Horizontal">
                        <Button Content="0" Style="{StaticResource WindowBtn}" Click="Minimize_Click"
                                FontFamily="Marlett" />
                        <Button Content="r" Style="{StaticResource CloseBtn}" Click="Close_Click" FontFamily="Marlett" />
                    </StackPanel>
                </StackPanel>
            </Grid>

            <!-- Content -->
            <ListView Grid.Row="1" ItemsSource="{Binding Accounts}"
                      Background="Transparent" BorderThickness="0"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      Padding="15">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" IsItemsHost="True" />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Margin" Value="6" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="BorderThickness" Value="0" />
                        <!-- Pass DataContext to ContextMenu -->
                        <Setter Property="Tag" Value="{Binding DataContext, ElementName=RootWindow}" />
                        <Setter Property="ContextMenu">
                            <Setter.Value>
                                <ContextMenu
                                    DataContext="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource Self}}">
                                    <MenuItem Header="Switch" Command="{Binding SwitchAccountCommand}"
                                              CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              FontWeight="Bold" />
                                    <MenuItem Header="Switch as...">
                                        <MenuItem Header="Online" Command="{Binding SwitchOnlineCommand}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        <MenuItem Header="Invisible" Command="{Binding SwitchInvisibleCommand}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        <MenuItem Header="Offline" Command="{Binding SwitchOfflineCommand}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    </MenuItem>
                                    <Separator />
                                    <MenuItem Header="Create Shortcut" Command="{Binding CreateShortcutCommand}"
                                              CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    <MenuItem Header="Open Game Data Folder" Command="{Binding OpenGameDataCommand}"
                                              CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    <Separator />
                                    <MenuItem Header="Copy SteamID">
                                        <MenuItem Header="SteamID64" Command="{Binding CopySteamId64Command}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        <MenuItem Header="SteamID3" Command="{Binding CopySteamId3Command}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        <MenuItem Header="Legacy ID" Command="{Binding CopySteamIdLegacyCommand}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    </MenuItem>
                                    <MenuItem Header="Copy Profile Link" Command="{Binding CopyProfileLinkCommand}"
                                              CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    <Separator />
                                    <MenuItem Header="Forget Account" Command="{Binding ForgetAccountCommand}"
                                              CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                </ContextMenu>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border x:Name="CardBorder" Background="#161616" CornerRadius="8"
                                            BorderBrush="#333333" BorderThickness="1"
                                            Width="260" Height="80" Margin="0" Cursor="Hand">
                                        <Border.InputBindings>
                                            <MouseBinding Gesture="LeftDoubleClick"
                                                          Command="{Binding DataContext.SwitchAccountCommand, ElementName=RootWindow}"
                                                          CommandParameter="{Binding}" />
                                        </Border.InputBindings>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="80" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>

                                            <!-- Avatar (ImageBrush for Rounding) -->
                                            <Border CornerRadius="7,0,0,7" BorderBrush="Transparent"
                                                    BorderThickness="0">
                                                <Border.Background>
                                                    <ImageBrush ImageSource="{Binding AvatarSource}"
                                                                Stretch="UniformToFill" />
                                                </Border.Background>
                                            </Border>

                                            <!-- Info -->
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0">
                                                <TextBlock Text="{Binding PersonaName}" Foreground="White"
                                                           FontWeight="SemiBold" FontSize="14"
                                                           TextTrimming="CharacterEllipsis"
                                                           ToolTip="{Binding PersonaName}" />
                                                <TextBlock Text="{Binding AccountName}" Foreground="#777777"
                                                           FontSize="12" TextTrimming="CharacterEllipsis"
                                                           Margin="0,2,0,0" />

                                                <!-- Tags -->
                                                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                                    <Border Background="#2A1111" CornerRadius="3" Padding="6,2"
                                                            Margin="0,0,6,0"
                                                            Visibility="{Binding IsVacBanned, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                        <TextBlock Text="VAC" Foreground="#FF5555" FontSize="10"
                                                                   FontWeight="Bold" />
                                                    </Border>
                                                    <Border Background="#2A2211" CornerRadius="3" Padding="6,2"
                                                            Visibility="{Binding IsLimited, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                        <TextBlock Text="LIMIT" Foreground="#FFCC00" FontSize="10"
                                                                   FontWeight="Bold" />
                                                    </Border>
                                                </StackPanel>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="CardBorder" Property="Background" Value="#222222" />
                                            <Setter TargetName="CardBorder" Property="BorderBrush" Value="#555555" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="CardBorder" Property="Background" Value="#333333" />
                                            <Setter TargetName="CardBorder" Property="BorderBrush" Value="#666666" />
                                        </Trigger>
                                        <DataTrigger Binding="{Binding IsActive}" Value="True">
                                            <Setter TargetName="CardBorder" Property="BorderBrush" Value="#FFFFFF" />
                                            <Setter TargetName="CardBorder" Property="BorderThickness" Value="1" />
                                            <Setter TargetName="CardBorder" Property="Background" Value="#1A1A1A" />
                                        </DataTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>

            <!-- Status Bar -->
            <Border Grid.Row="2" Background="Transparent" Padding="20,0">
                <Grid VerticalAlignment="Center">
                    <TextBlock Text="{Binding StatusMessage}" Foreground="#DDDDDD" FontSize="14"
                               HorizontalAlignment="Left" />
                    <ProgressBar IsIndeterminate="True" Width="100" Height="2" HorizontalAlignment="Right"
                                 Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                                 Background="#222222" BorderThickness="0" Foreground="White" />
                </Grid>
            </Border>

        </Grid>
    </Border>
</Window>